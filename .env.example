# Supabase — preencha quando criarmos o projeto na nuvem.
# Copie este arquivo para .env.local e cole suas chaves.
# Enquanto vazio, o app usa armazenamento local (localStorage) no navegador.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
